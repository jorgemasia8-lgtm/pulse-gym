export const esc=v=>String(v??'').replace(/[&<>"']/g,c=>({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;',"'":'&#39;'}[c]));export const fmtSec=s=>`${Math.floor(s/60)}:${String(Math.max(0,s%60)).padStart(2,'0')}`;export const fmtDate=d=>new Intl.DateTimeFormat('es-ES',{day:'numeric',month:'short',year:'numeric'}).format(new Date(d));export const num=n=>Number(n||0).toLocaleString('es-ES',{maximumFractionDigits:1});export const e1rm=(w,r)=>+(w*(1+r/30)).toFixed(1);export const volume=logs=>logs.filter(x=>x.done).reduce((a,x)=>a+(+x.weight||0)*(+x.reps||0),0);export const rangeMax=r=>+String(r||'').match(/(\d+)\s*$/)?.[1]||0;export const uid=()=>crypto.randomUUID();