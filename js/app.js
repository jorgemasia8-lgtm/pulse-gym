import {routines,todayRoutine,findRoutine} from './routines.js';import {getActive,startSession,completeSet,addSet,nextExercise,finishSession,sessionVolume,lastExerciseLog} from './session.js';import {startTimer,format} from './timer.js';import {sessions,dateLabel,duration} from './history.js';import {settings,saveSettings,exportJSON} from './settings.js';import {nav,card} from './ui.js';
const app=document.querySelector('#app');let route='home',detail=null,timerStop=null,timerLeft=0;const esc=s=>String(s).replace(/[&<>"']/g,c=>({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;',"'":'&#39;'}[c]));function totalSets(r){return r.exercises.reduce((n,e)=>n+e.sets,0)}function durationLive(s){return format(Math.floor((Date.now()-s.startedAt)/1000))}
async function render(){const active=getActive();let html='';if(route==='home'){const r=todayRoutine();html=`<div class="top"><div><div class="eyebrow">Pulse Gym</div><h1 class="title">Hola, Jorge</h1><div class="muted">${new Intl.DateTimeFormat('es-ES',{weekday:'long',day:'numeric',month:'long'}).format(new Date())}</div></div><span class="pill">Modo gym</span></div><section class="card hero"><div class="eyebrow">Hoy te toca</div><h2 class="title">${r.name}</h2><p class="muted">${r.exercises.length?`${r.exercises.length} ejercicios · ${totalSets(r)} series`:'Recuperación activa'}</p><div style="height:14px"></div><button class="btn" data-start="${r.id}">${r.exercises.length?'Empezar entrenamiento':'Ver plan de descanso'}</button></section>${active?`<section class="card"><b>Sesión en curso</b><p class="muted">${active.routineName} · ${durationLive(active)}</p><button class="btn secondary" data-route="session">Reanudar sesión</button></section>`:''}${nav('home')}`}
else if(route==='routines'){html=`<div class="top"><div><div class="eyebrow">Plan semanal</div><h1 class="title">Rutinas</h1></div></div>${routines.map(r=>`<article class="card routine" data-detail="${r.id}"><div class="row"><div><h3>${r.dayName}</h3><p class="muted">${r.name}</p><span class="pill">${r.exercises.length} ejercicios</span></div><button class="btn small" data-start="${r.id}">${r.exercises.length?'Entrenar':'Ver'}</button></div></article>`).join('')}${nav('routines')}`}
else if(route==='detail'){const r=findRoutine(detail);html=`<div class="top"><button class="btn secondary small" data-route="routines">← Volver</button></div><div class="eyebrow">${r.dayName}</div><h1 class="title">${r.name}</h1><p class="muted">${r.cardio}</p>${r.exercises.map((e,i)=>card(`${i+1}. ${e.name}`,`<div class="muted">${e.sets} series · ${e.reps} reps · Descanso ${e.rest}s</div>`)).join('')}<button class="btn" data-start="${r.id}">Iniciar sesión</button>`}
else if(route==='session'){if(!active){html=`<div class="empty"><h2>No hay sesión activa</h2><p>Elige una rutina para empezar.</p><button class="btn" data-route="routines">Ver rutinas</button></div>${nav('session')}`}else{const ex=active.exercises[active.exerciseIndex];const last=await lastExerciseLog(ex.name);const done=active.exercises.filter(e=>e.logs.every(x=>x.done)).length;html=`<div class="session-head"><div class="row"><button class="btn secondary small" data-route="home">←</button><div class="muted">${durationLive(active)}</div></div><div class="eyebrow">Ejercicio ${active.exerciseIndex+1} de ${active.exercises.length}</div><h1 class="title">${ex.name}</h1><div class="muted">${last?`Último: ${last.weight} ${ (await settings()).unit} × ${last.reps}`:`Objetivo: ${ex.sets} × ${ex.reps}`}</div><div class="progress"><i style="width:${(done/active.exercises.length)*100}%"></i></div></div><section class="card exercise"><div class="row"><b>Series</b><span class="pill">Descanso ${ex.rest}s</span></div><table class="sets"><thead><tr><th>SET</th><th>PESO</th><th>REPS</th><th>OK</th></tr></thead><tbody>${ex.logs.map((l,i)=>`<tr><td>${l.set}</td><td><input inputmode="decimal" type="number" min="0" step="0.5" value="${l.weight}" data-weight="${i}"></td><td><input inputmode="numeric" type="number" min="0" value="${l.reps}" data-reps="${i}"></td><td><button class="complete ${l.done?'done':''}" data-complete="${i}" ${l.done?'disabled':''}>${l.done?'✓':'○'}</button></td></tr>`).join('')}</tbody></table><div style="height:10px"></div><button class="btn secondary" data-addset>Añadir serie</button></section><div class="stack"><button class="btn" data-next>${active.exerciseIndex===active.exercises.length-1?'Finalizar entrenamiento':'Siguiente ejercicio'}</button><button class="btn danger" data-finish>Finalizar y guardar</button></div>${timerLeft?`<aside class="timer"><div class="eyebrow">Descanso</div><div class="timer-num">${format(timerLeft)}</div><div class="muted">${ex.name} · Serie completada</div><div class="row" style="margin-top:12px"><button class="btn secondary small" data-timer="-15">−15 s</button><button class="btn secondary small" data-timer="15">+15 s</button><button class="btn small" data-timer="skip">Saltar</button></div></aside>`:''}${nav('session')}`}}
else if(route==='summary'){const s=window.lastFinished;html=`<div class="empty"><div class="eyebrow">Entreno completado</div><h1 class="title">Buen trabajo ✓</h1><p class="muted">${s.routineName}</p><div class="card" style="text-align:left"><div class="row"><span>Duración</span><b>${duration(s.duration)}</b></div><hr><div class="row"><span>Series</span><b>${s.series}</b></div><hr><div class="row"><span>Volumen</span><b>${s.volume.toLocaleString('es-ES')} kg</b></div></div><button class="btn" data-route="history">Ver historial</button><div style="height:10px"></div><button class="btn secondary" data-route="home">Volver al inicio</button></div>`}
else if(route==='history'){const all=await sessions();html=`<div class="top"><div><div class="eyebrow">Registro</div><h1 class="title">Historial</h1></div></div>${all.length?all.map(s=>`<article class="card"><h3 class="history-title">${esc(s.routineName)}</h3><p class="muted">${dateLabel(s.date)} · ${duration(s.duration)}</p><div class="row"><span class="pill">${s.series} series</span><b>${s.volume.toLocaleString('es-ES')} kg</b></div></article>`).join(''):`<div class="empty"><h2>Aún no hay entrenos</h2><p>Completa tu primera sesión y aparecerá aquí.</p></div>`}${nav('history')}`}
else if(route==='settings'){const set=await settings();html=`<div class="top"><div><div class="eyebrow">Preferencias</div><h1 class="title">Ajustes</h1></div></div><section class="card"><h3>Unidad</h3><div class="row"><button class="btn ${set.unit==='kg'?'':'secondary'}" data-unit="kg">kg</button><button class="btn ${set.unit==='lb'?'':'secondary'}" data-unit="lb">lb</button></div></section><section class="card"><div class="row"><div><h3>Sonido</h3><p class="muted">Al terminar descanso</p></div><button class="toggle ${set.sound?'on':''}" data-toggle="sound"></button></div><div class="row" style="margin-top:18px"><div><h3>Vibración</h3><p class="muted">Aviso táctil</p></div><button class="toggle ${set.vibration?'on':''}" data-toggle="vibration"></button></div></section><section class="card"><h3>Descansos por defecto</h3><p class="muted">Básicos: ${set.basicRest}s · Accesorios: ${set.accessoryRest}s</p></section><button class="btn secondary" data-export>Exportar datos JSON</button>${nav('settings')}`}
app.innerHTML=`<main class="app">${html}</main>`;bind()}
function beginTimer(sec){timerStop?.();timerLeft=sec;timerStop=startTimer(sec,x=>{timerLeft=x;render()},()=>{timerLeft=0;render();});}
function bind(){document.querySelectorAll('[data-route]').forEach(b=>b.onclick=()=>{route=b.dataset.route;render()});document.querySelectorAll('[data-detail]').forEach(x=>x.onclick=e=>{if(e.target.dataset.start)return;detail=x.dataset.detail;route='detail';render()});document.querySelectorAll('[data-start]').forEach(b=>b.onclick=()=>{startSession(findRoutine(b.dataset.start));route='session';render()});document.querySelectorAll('[data-complete]').forEach(b=>b.onclick=()=>{const i=+b.dataset.complete,w=document.querySelector(`[data-weight="${i}"]`).value,r=document.querySelector(`[data-reps="${i}"]`).value;completeSet(i,w,r);beginTimer(getActive().exercises[getActive().exerciseIndex].rest);render()});document.querySelector('[data-addset]')?.addEventListener('click',()=>{addSet();render()});document.querySelector('[data-next]')?.addEventListener('click',async()=>{const s=getActive();if(s.exerciseIndex===s.exercises.length-1){window.lastFinished=await finishSession();timerStop?.();timerLeft=0;route='summary'}else nextExercise();render()});document.querySelector('[data-finish]')?.addEventListener('click',async()=>{window.lastFinished=await finishSession();timerStop?.();timerLeft=0;route='summary';render()});document.querySelectorAll('[data-timer]').forEach(b=>b.onclick=()=>{if(b.dataset.timer==='skip'){timerStop?.();timerLeft=0;render()}else beginTimer(Math.max(0,timerLeft+ +b.dataset.timer))});document.querySelectorAll('[data-unit]').forEach(b=>b.onclick=async()=>{await saveSettings({unit:b.dataset.unit});render()});document.querySelectorAll('[data-toggle]').forEach(b=>b.onclick=async()=>{const s=await settings();await saveSettings({[b.dataset.toggle]:!s[b.dataset.toggle]});render()});document.querySelector('[data-export]')?.addEventListener('click',exportJSON)}
if('serviceWorker' in navigator)navigator.serviceWorker.register('./service-worker.js');render();