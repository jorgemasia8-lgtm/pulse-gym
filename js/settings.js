import {get,put} from './db.js';const base={id:'app',unit:'kg',sound:true,vibration:true,basicRest:150,accessoryRest:90};export async function settings(){return (await get('settings','app'))||base} export async function saveSettings(v){return put('settings',{...base,...v,id:'app'})} export async function exportJSON(){const {getAll}=await import('./db.js');const data={sessions:await getAll('sessions'),settings:await settings()};const a=document.createElement('a');a.href=URL.createObjectURL(new Blob([JSON.stringify(data,null,2)],{type:'application/json'}));a.download='pulse-gym-backup.json';a.click();URL.revokeObjectURL(a.href)}