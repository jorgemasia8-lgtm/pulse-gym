let interval; export function startTimer(seconds,onTick,onDone){clearInterval(interval);let left=seconds;onTick(left);interval=setInterval(()=>{left--;onTick(Math.max(0,left));if(left<=0){clearInterval(interval);navigator.vibrate?.([180,100,180]);onDone?.();}},1000);return()=>clearInterval(interval)} export const format=t=>`${String(Math.floor(t/60)).padStart(2,'0')}:${String(t%60).padStart(2,'0')}`;