export const icon={home:'⌂',routines:'▦',session:'◉',history:'◷',settings:'⚙'}; export function nav(active){return `<nav class="nav"><div class="nav-inner">${[['home','Inicio'],['routines','Rutinas'],['session','Sesión'],['history','Historial'],['settings','Ajustes']].map(([id,l])=>`<button class="tab ${active===id?'active':''}" data-route="${id}"><b>${icon[id]}</b><br>${l}</button>`).join('')}</div></nav>`} export function card(title,body,cls=''){return `<article class="card ${cls}"><h3>${title}</h3>${body}</article>`}